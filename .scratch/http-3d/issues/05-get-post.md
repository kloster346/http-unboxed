# 05: GET vs POST 双场景 + 场景切换

**What to build:** 顶部场景切换（主流程 ↔ GET vs POST）；两个并排 3D 小场景展示差异：GET 取件（参数在 URL、无包裹、明文、幂等）与 POST 寄件（货在 Body、带 Token、较安全、非幂等）；各配演示动画与差异字幕。

**Blocked by:** 03 数据球沿轨迹飞行 + 相机跟拍（与 04 并行）

**Status:** ready-for-agent

- [ ] 顶部能在主流程与 GET vs POST 场景间切换
- [ ] GET 场景表现「参数在 URL / 无包裹 / 明文 / 幂等」
- [ ] POST 场景表现「货在 Body / 带 Token / 较安全 / 非幂等」
- [ ] 两个场景各配演示动画与差异字幕
