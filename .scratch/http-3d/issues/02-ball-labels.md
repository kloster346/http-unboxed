# 02: 主流程——生成数据球 + 贴面单

**What to build:** 主流程第一幕：手机节点 + 点「下单」，生成一颗发光数据球，数据球带 `JSON` 标签，并贴上 `URL`、`Header·Token` 面单标签；标签随对应幕次出现/消失。

**Blocked by:** 01 工程骨架 + 首个 3D 场景

**Status:** ready-for-agent

- [ ] 点「下单」后数据球出现并带 `JSON` 标签
- [ ] `URL` / `Header·Token` 面单标签随对应幕次出现
- [ ] 所在幕次字幕正确
- [ ] `useFlow` 测试覆盖新增幕次（生成、贴标）
