# Spec：给 PM 的 HTTP/API「3D 动画」演示

> 依据：`/grill-with-docs` 已完成的决策树 + `/to-spec` 综合。所有决策以本 spec 为准。

## Problem Statement

用户（一个帮不懂技术的产品经理讲前端/后端通信的开发者，Java 后端实习背景、前端经验偏弱）已经做出两个版本：

1. **图文交互版**（单 HTML 文件）——用"寄快递"类比讲 URL / Header / Body / JSON / GET / POST。
2. **平面 SVG 动画版**（单 HTML 文件）——把"一次通信"做成包裹沿虚线飞过快递站、进仓库、回传签收单。

用户认为两个版本仍"过于单调、不够抓人"，希望做一个**真正的 3D 动画**版本，技术栈大幅升级（从纯 HTML 升级到现代 3D Web 工程），并且保持"**文字越少越好**、靠视觉流动讲概念"的核心诉求。目标受众仍是不懂技术的产品经理。

## Solution

用 **React Three Fiber（R3F）+ Three.js + Vite + TypeScript** 构建的 3D 动画演示应用。

抽象"数据流"视觉（不依赖外部 3D 模型）：一次通信被表现为一颗**发光数据球**——贴上面单（`URL` + `Header·Token` 标签），沿橙色发光轨迹飞出，经过**网络节点（快递站）**，落进**服务器节点（仓库）**，拆包/计算后回传一颗**「200 OK」签收球**沿蓝色轨迹飞回手机，手机亮起"已收货"。

- **相机**：默认电影式跟拍（相机跟随数据球）；可暂停后 **OrbitControls 拖拽环视**。
- **交互**：顶部控制条 `⏮上一步 / ▶自动播(暂停) / 下一步⏭ / ↺重播` + 场景切换（主流程 / GET vs POST）。
- **副场景**：GET vs POST 双 3D 场景对比（左：GET 取件=参数在 URL、无包裹、明文/幂等；右：POST 寄件=货在 Body、带 Token、较安全/非幂等）。
- **交付**：`npm run dev` 本地开发；`npm run build` 产出纯静态 `dist/`（本地静态 server 或内网即可演示，暂不做公网上线）。

## User Stories

1. 作为演示者，我想要一个在浏览器里直接运行的 3D 场景，以便 PM 无需装任何环境即可观看。
2. 作为演示者，我想用抽象的"发光数据球"代表数据，以便 PM 不看文字就能感知"数据在动"。
3. 作为演示者，我想让数据球贴上 `URL` 与 `Header·Token` 面单标签，以便 PM 把"地址"和"身份验证"对上快递面单。
4. 作为演示者，我想让数据球沿一条发光的轨迹从手机飞向网络节点，以便 PM 直观理解"请求发出的路径"。
5. 作为演示者，我想让数据球进入服务器节点后"拆包/处理"并有齿轮旋转动画，以便 PM 理解"服务器在处理请求"。
6. 作为演示者，我想让服务器回传一颗标注 `200 OK` 的签收球飞回手机，以便 PM 理解"后端响应"。
7. 作为演示者，我想默认使用电影式跟拍相机，以便 PM 无需操作就能看完整个流程。
8. 作为演示者，我想在暂停后允许 PM 拖拽旋转/缩放相机，以便 PM 能近距离观察某个节点。
9. 作为演示者，我想提供「上一步/下一步/自动播/重播」控制，以便我现场边讲边推、或让它自动播放。
10. 作为演示者，我想在顶部一键切换「主流程」与「GET vs POST」两个场景，以便对照讲解查询与提交的差异。
11. 作为演示者，我想在 GET 场景里体现"参数写在 URL、无包裹、明文、幂等、有长度限制"，以便 PM 记住 GET=查。
12. 作为演示者，我想在 POST 场景里体现"货在 Body、带 Token、较安全、非幂等、量大"，以便 PM 记住 POST=增/改。
13. 作为演示者，我想整个页面只保留一步一句的极简字幕，以便 PM 不被文字淹没、聚焦动画。
14. 作为演示者，我想让整个应用不依赖任何外部下载的 3D 模型，以便工程自包含、随时能跑。
15. 作为演示者，我想让动画带发光/后处理效果（Bloom），以便视觉更"科技、高级"。
16. 作为演示者，我想用一个状态机驱动"第几幕"，以便控制条、相机、字幕、高光都在同一节奏上。
17. 作为开发者，我想用 React + TypeScript + R3F 的声明式场景结构，以便后续扩展新场景/新节点更清晰。
18. 作为开发者，我想用 GSAP 编排动画序列，以便包裹飞行/拆包/回传等动画可控、可串联。
19. 作为开发者，我想工程里状态的唯一事实来源是一个 `useFlow` 状态机 hook，以便逻辑可测试、可复用。
20. 作为开发者，我想 `npm run build` 产出纯静态文件，以便能放到任意静态托管/本地 server 交付给 PM。
21. 作为开发者，我想在不联网时也能至少跑起来（本地依赖完整），以便现场演示不出幺蛾子。
22. 作为 PM，我想在演示结束时看到一句话总结（前端=寄快递、后端=回签收单），以便我记住核心。
23. 作为 PM，我想在 GET 与 POST 场景旁看到一句话的差异总结，以便我日后给开发提需求时用词准确。
24. 作为演示者，我想支持回退到上一步时状态正确复位，以便讲解过程中随时重放某一段。

## Implementation Decisions

- **技术栈**：React 18 + TypeScript + Vite + `@react-three/fiber`（R3F）+ `@react-three/drei` + `@react-three/postprocessing`（Bloom 发光）+ `gsap` / `@gsap/react`（useGSAP 编排动画）+ `three`。
- **模块划分**：
  - 场景组件：手机节点、数据球/包裹、网络节点、服务器节点、飞行轨迹、后处理。
  - 场景容器：`主流程一次通信` 与 `GET vs POST 对比` 两个独立场景，支持顶部切换。
  - 状态机：一个 `useFlow` 状态机 hook，作为流程的唯一事实来源。
  - 界面组件：控制条（上一步/下一步/播放暂停/重播/场景切换）与一步字字幕层。
  - 数据：幕次字幕与每步配置（只读数据）。
- **`useFlow` 接口（契约）**：暴露 `{ step, steps, prev, next, replay, play, pause, isPlaying, scene, setScene }`；其中 `next/prev/replay` 是唯一能改变流程状态的入口；`step` 为只读当前幕次索引；`steps` 为只读幕次表。场景切换 `setScene` 独立于幕次推进。
- **相机决策**：默认电影式跟拍（相机目标随数据球移动）；暂停后启用 OrbitControls 供拖拽环视；两者并非同一时刻启用，避免操控冲突。
- **视觉决策**：全部用几何体 + 粒子 + 发光材质表达，不加载外部 GLTF/贴图模型；用 drei 的 `Text`/`Environment`/`OrbitControls`；Bloom 后处理做发光。极简文字：只用标签词与一步一句字幕。
- **交付决策**：`npm run dev` 开发；`npm run build` → 纯静态 `dist/`，可本地静态 server 或内网演示；`base` 路径配置为相对路径以兼容任意静态托管。
- **工程决策**：新建独立子目录工程（不复用之前的 HTML 交付物）；TypeScript 开启严格模式；依赖锁定以重现安装。
- **状态模型**（源自此前原型/讨论出的分幕，作为契约而非完整代码）：幕次按「下单→生成数据球并贴面单→飞出→过网络→进服务器→拆包处理→回传签收球→完成」顺序推进，每幕有极简字幕、相机目标、节点高光三个可观察输出。

## Testing Decisions

- **好测试的定义**：只测外部可观察行为（用户能感知的状态），不测内部实现（不 mock 相机/不测场景图内部结构）。此工程是强视觉、强动画的 R3F 应用，R3F 场景图不适合做细粒度单元断言。
- **测试 seam**：**唯一的、最高的 seam 是 `useFlow` 状态机 hook**。它承载所有流程逻辑（幕次、前进/后退/重播、播放状态、场景切换、字幕输出），且与 3D 渲染解耦，可用纯逻辑测试覆盖。新 seam 尽量不再引入，除非未来出现可复用的纯逻辑模块（如步骤数据校验）。
- **将被测试的模块**：`useFlow`（默认导出与各状态转换；next/prev/replay 的边界与复位；播放/暂停切换；场景切换不污染幕次）。
- **测试内容**（外部行为）：初始幕次、`next` 到底后不再前进、`prev` 到顶后不再后退、`replay` 归零、字幕文本随幕次正确变化、`setScene` 切场景后幕次保持、播放起来后按节奏自动 `next`、到底自动停。
- **Prior art**：本项目为新工程，无既有测试可参照。工具链采用 **Vitest + @testing-library/react**（R3F/React 生态标准），`useFlow` 用 `renderHook` 测。

## Out of Scope

- 真实后端 / 真实 API 调用与响应（纯前端演示，数据全 mock）。
- 写实 3D 建模、外部 GLTF/贴图/骨骼动画资产。
- WebXR / AR / VR。
- 移动端深度适配与小屏优化（首要在桌面演示）。
- 公网上线部署（GitHub Pages / Vercel / Netlify / 自有服务器）——当前只做本地静态交付；上线可在后续用 `/setup-matt-pocock-skills` 或单独需求补齐。
- 把「对照表」内嵌进 3D 场景（会引入文字、违背"文字最少"）；对照表继续保留在图文版。
- i18n / 多语言。
- 不直接修改之前两个 HTML 交付物（新工程独立迭代）。

## Further Notes

- 已确认运行环境具备 **Node v22.23.2、npm 11.3.0、npx 11.3.0**，R3F + Vite 工具链可直接使用。
- **issue tracker / triage 标签词汇未配置**：本 spec 无法自动发布到工单系统并打 `ready-for-agent`。如需自动发布，请先运行 `/setup-matt-pocock-skills`。当前以本 Markdown 文件作为 spec 交付物。
- 环境备注：DSH 共享浏览器里 `browser_screenshot` 在 JS 触发 CSS 动画/过渡后会返回陈旧帧（同 sha 缓存），验证动画逻辑应使用 `getComputedStyle` / `browser_execute`，而非依赖截图。
- 技术调研共识（供实现参考）：Three.js 为 WebGL 事实基底；React 生态声明式 3D 行业标准为 R3F（配 Drei / R3F-postprocessing）；动画编排用 GSAP；Babylon.js 偏企业级 XR，本需求不用。
