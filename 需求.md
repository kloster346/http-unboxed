给不懂技术的产品经理讲技术，**千万别从“什么是JSON语法”切入**，那会直接劝退。**最有效的切入点是“寄快递”**——这个场景他们天天用，能无缝映射所有概念。

按这个顺序讲，保证他秒懂：

**1. 先画大流程图（2分钟）**
在白板上画：**“手机（你） → 快递站（网络） → 仓库（服务器）”**。
告诉他：**前端请求 = 填快递单发货，后端响应 = 仓库回传签收单**。接下来的所有名词，都是这张快递单上的不同栏目。

**2. 逐个击破（结合快递单）**

- **URL（地址栏）** = **快递收件地址**。比如 `https://api.xxx.com/order`，前半段是“哪个园区（域名）”，后半段是“几号楼几零几（路径）”。**问号后面的参数（?page=1）** = **备注栏**（比如“放门口鞋柜”），用来给快递员看的分拣小标签。
- **请求头（Header）** = **快递面单的外露信息**（寄件人手机号、实名认证、加急标识）。它**存放“元数据”**，比如登录令牌（Token，相当于身份证）、数据格式声明（告诉服务器“我发的是中文还是英文”）。**关键话术**：“Header负责‘怎么寄’，告诉服务器验明正身和运输规格。”
- **请求体（Body）** = **快递包裹里的实际货物**。只有“寄件”（POST/PUT）时才需要填，如果是“取件”（GET），包裹是空的。
- **JSON** = **包裹里的标准化填充物（或说明书）**。比如 `{"user":"张三","price":99}`。强调**不必纠结花括号和冒号**，告诉PM：“这就是一种‘键值对’清单，前端和后端都按这个清单拆包，比乱塞衣服好整理得多。” 顺便提一嘴，如果JSON里字段名写错（比如把`userId`写成`userID`），等于把“收件人”写错，仓库会直接拒收（报错）。

**3. 重点区分 GET 和 POST（灵魂拷问区）**

- **GET（查）** = **去快递柜取件**。把取件码（参数）直接写在地址栏（URL）上。**特点**：有长度限制（快递柜格子有限）、明文不安全（谁路过都能看到取件码）、**幂等**（取一次不会让柜子多出一个包裹）。
- **POST（增/改）** = **去前台寄包裹**。货物（Body）藏在包里，**特点**：体积大（传图片/长文本）、相对安全（包着看不见）、**非幂等**（寄一次多一个包裹，重复寄两次就多两个）。
- **其他（PUT改全部/PATCH改部分/DELETE删）** = **换货/改收货地址/退货**。告诉PM：“前端开发中90%只用GET和POST，剩下的是特殊场景优化，知道概念即可。”

**4. 给PM的终极“避坑指南”（他最需要的）**

- **调试看报错**：接口报错`400`（请求头/体格式不对）→ 检查JSON是不是多了逗号；报错`401/403`（请求头Token失效）→ 重新登录；报错`404`（URL路径写错）→ 检查地址栏单词拼写。
- **给开发提需求**：以后别说“我要调这个接口”，要说“**调这个URL路径的POST接口，Body里传JSON格式的订单数据，Header里带用户Token**”。

**最后，一定给他看一张“对照表”**（拍照给他）：把URL、Header、Body、JSON、GET/POST列成一行，右边写上你们公司实际业务的案例（比如“获取商品列表”对应GET+URL参数；“提交订单”对应POST+Header Token+Body JSON）。